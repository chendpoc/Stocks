# Trader Agent Shared Contracts

Shared product-level contracts for the `apps/trader-agent/` subproject.

Expected contents:

- `rulepacks/`: canonical RulePack files.
- `schemas/`: API and event schema definitions shared by backend and cockpit.

