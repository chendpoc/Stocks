/**
 * Longbridge 组工具 — compatibility barrel.
 *
 * Implementation lives under `./longbridge/`; this file re-exports
 * `LONGBRIDGE_TOOLS` for existing imports (e.g. toolRegistry.bootstrap.ts).
 */

export { LONGBRIDGE_TOOLS } from "./longbridge/index.js";
