# Active Specs

Historical completed or superseded specs were moved to
`project-docs/archive/agent-dev/specs/`.

New specs should include `spec.md`, `spec.json`, and `decision-record.json`
when the selected workflow requires them.
