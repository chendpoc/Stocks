# Slice T002-P2-S4 — CLI report command

**May edit:** `apps/trader-cli/src/commands/report.ts`, `apps/trader-cli/src/index.ts`

**Depends:** P2-S2 `/report/check` + `/save`

**Acceptance (V104):**
```bash
cd apps/trader-cli && npx tsx src/index.ts report TSLA && npx tsx src/index.ts report TSLA
```
第二次输出含 `[缓存命中]`。
