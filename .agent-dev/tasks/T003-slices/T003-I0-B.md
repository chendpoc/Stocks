# Slice T003-I0-B — services 共享层

**May edit:** `apps/trader-cli/src/services/**` only

**Do NOT edit:** `tui/**`, `commands/**`, backend

**Depends on:** T003-I0-A

**Acceptance:**
```bash
cd apps/trader-cli && npm test
```
