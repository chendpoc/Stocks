# Slice T003-I2-B — Dashboard report + hints

**May edit:** `DashboardPage.tsx`（`[g]` report）, `menu.ts` MENU_HINTS, `ContextHint.tsx`, 可选 `chatSuggestions.ts`

**Do NOT edit:** `ChatPage.tsx` 核心逻辑

**Depends on:** T003-I2-A

**Acceptance:** Dashboard `[g]` 生成/命中缓存；hint 无「后续可接」占位
