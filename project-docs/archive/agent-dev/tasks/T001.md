# T001: Forward Market Intelligence MVP

> 双文件原则：本文件给人类审阅。结构化约束在 `T001.json`。
> JSON Schema: `.agent-dev/memory/schemas.md §3`

## 步骤概览

```
P0 (Schema) → P1 (行情) → P2 (特征) → P3 (事件) → P4 (上下文)
  ↓
P5 (交易) → P6 (复盘) → P7 (Jobs) → P8 (CLI) → P9 (配置)
```

全部 10 步的详细实施指南见：

[Legacy plan: project-docs/archive/forward-intel/03-forward-market-intel-mvp-plan.md](../../forward-intel/03-forward-market-intel-mvp-plan.md)

## 步骤验证

每个步骤的验证方式见 `T001.json` 的 `steps[].verification.ref`，指向 `spec.json` 的 `verification[].id`。

## 依赖关系

```
P0 (无前置)
  → P1 (依赖 P0)
    → P2 (依赖 P1)
      → P3 (依赖 P1)
        → P4 (依赖 P2 + P3)
          → P5 (依赖 P4)
            → P6 (依赖 P5)
              → P7 (依赖 P4)
                → P8 (依赖 P4)
                  → P9 (依赖 P8)
```

## 相关文档

- Spec: `../specs/forward-market-intel/spec.json`
- Legacy Plan: `project-docs/archive/forward-intel/03-forward-market-intel-mvp-plan.md`
