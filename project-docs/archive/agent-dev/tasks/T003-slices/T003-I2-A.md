# Slice T003-I2-A — Dashboard scan + chart

**May edit:** `DashboardPage.tsx`, `app.tsx`（focusedSymbol）

**Do NOT edit:** report `[g]` 逻辑（I2-B）

**Depends on:** T003-I1-D

**Acceptance:** Dashboard `[/]` 换标的；`[s]` scan 摘要；有 bars 时显示 ASCII chart
