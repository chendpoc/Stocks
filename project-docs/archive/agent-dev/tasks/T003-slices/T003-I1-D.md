# Slice T003-I1-D — SignalsPage + lastScan

**May edit:** `SignalsPage.tsx`, `app.tsx`（lastScan state props）, `ScanSummary.tsx`, `ContentArea.tsx`（props 透传）

**Do NOT edit:** `DashboardPage`（I2）

**Depends on:** T003-I1-C

**Acceptance:** Signals `[s]` 后 `ScanSummary` 显示 pattern/cross 计数
