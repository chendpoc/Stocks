# Slice T002-P1-S2 — Chat TUI + tools

**May edit:** `src/tui/pages/ChatPage.tsx`, `src/commands/chat.ts`, `src/llm/tools.ts` (getRelatedHypotheses), `src/index.ts`

**Also:** 修复 chat.ts mojibake（UTF-8）

**Anchors:** D108, D112

**Acceptance:**
```bash
cd apps/trader-cli && npx tsx src/index.ts chat
cd apps/trader-cli && npx tsx src/index.ts chat --eval "列出 TSLA 之前的假设记录"
```
