# Slice T002-P4 — chart + server

**May edit:** `commands/chart.ts`, `commands/server.ts`, `package.json`, `index.ts`

**Anchors:** D106, D107

**Acceptance (V107, V103):**
```bash
cd apps/trader-cli && npx tsx src/index.ts server status
cd apps/trader-cli && npx tsx src/index.ts chart TSLA
```
