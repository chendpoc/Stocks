{
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "daily:notify": "node scripts/run-daily-notify.mjs",
    "daily:notify:dry": "node scripts/run-daily-notify.mjs --dry"
  }
}