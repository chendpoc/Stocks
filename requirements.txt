# Pinned ranges for reproducible installs. numpy<2 avoids Windows binary ABI issues with some wheels (e.g. older pyarrow) against NumPy 2.x.
numpy>=1.26.4,<2.0
openai>=1.40.0,<2
requests>=2.31.0,<3
loguru>=0.7.2
pytz>=2024.1
pandas>=2.2.3,<3
pandas-market-calendars>=4.4.0,<6
google-genai>=1.0.0,<2
websockets>=12.0,<16

# If mirror lacks packages: pip install -r requirements.txt -i https://pypi.org/simple
