openai>=1.0.0
requests
loguru
pytz
pandas
pandas_market_calendars
# 国内镜像若缺包，请使用：pip install -r requirements.txt -i https://pypi.org/simple
# 若导入 pandas/pyarrow 报 NumPy 版本冲突，可在干净 venv 中安装，或临时约束：numpy>=1.26,<2
google-genai
